/* Input two numbers and output their sum */
#include <stdio.h>
int main() {
    int num1, num2, sum;
    scanf("%d %d", &num1, &num2); //taking input
    sum = num1 + num2;  //adding them
    printf("Sum = %d\n",sum); //printing their sum
    return 0;
}
